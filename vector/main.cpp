#include "vector.h"
#include <vector>
#include <iostream>

int main(){
    jiao::vector<int> v1;
    std::vector<int> v2;

    v1.push_back(1);
    v2.push_back(1);

    v1.insert(v1.begin(), 2);   
    v2.insert(v2.begin(), 2);

    v2.

    return 0;
}